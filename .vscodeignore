.vscode/**
.vscode-test/**
src/**
out/**
node_modules/**
.gitignore
esbuild.js
tsconfig.json
**/*.map
**/.eslintrc.json

# Runtime data scaffolded by the extension itself if this repo's own root ever
# gets opened as a workspace while testing — never intended to ship.
.ariadne/**
.fde/**
